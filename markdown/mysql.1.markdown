
#  mysql


## common commands

Access to mysql:
`mysql -u username --protocol=tcp -h hostname -p`

To use a table:
`use TABLE \g`

To query:
`select * from TABLE where FIELD like '%something%' \g`




